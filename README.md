# ProyectoIA

pip install streamlit numpy matplotlib seaborn
python -m pip install streamlit numpy matplotlib seaborn
py -m pip install streamlit numpy matplotlib seaborn

import streamlit as st

st.title("Proyecto de Machine Learning desde Cero")
st.success("¡La interfaz web está funcionando perfectamente!")

streamlit run app.py
